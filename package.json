{
  "private": true,
  "workspaces": [
    "packages/npm/*"
  ],
  "license": "MIT",
  "scripts": {
    "build:rust": "cargo build --release",
    "test:rust": "cargo test",
    "format": "cargo fmt",
    "format:check": "cargo fmt --check"
  }
}
